{% extends "GestionProjetBundle::base.html.twig" %}


{% block A %}
    <div class="row">

        <div class="col-md-3">
            <p class="lead">Listes Des Categories</p>
            <div class="list-group">
                <a href="{{path('article_new')}}" class="list-group-item">Ajouter</a>
                <a href="#"class="list-group-item">editer</a>
                <a href="{{path('article_index')}}" class="list-group-item">afficher</a>
            </div>
        </div>




    <div class="col-md-9">
    <h1>Article</h1>

    <table class ="table table-striped" >
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article.description }}</td>
            </tr>
            <tr>
                <th>Auteurs</th>
                 <td> {{article.nom}}</td>
                
                
            </tr>
            
        </tbody>
    </table>

    <ul>
                <li>
                    {#{ form_start(delete_form) }}
                    <input type="submit" value="Delete">
                    {{ form_end(delete_form) }#}
                </li>
            </ul>

        </div>    
    </div>
{% endblock A %}   
